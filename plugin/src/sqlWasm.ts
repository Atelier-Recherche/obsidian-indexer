/** Inclus dans main.js par esbuild (loader `binary`) — évite tout fetch vers le dossier du plugin. */
import sqlWasmBrowser from "../node_modules/sql.js/dist/sql-wasm-browser.wasm";

export default sqlWasmBrowser;
